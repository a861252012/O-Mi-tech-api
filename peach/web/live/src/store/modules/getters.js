/**
 *   Created by zeal on 2017/11/30
 */
export const getters = {
    userInfo: state => {
        return state.userInfo
    },
    categoryList: state => {
        return state.categoryList
    }
}