<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
    <script type="text/javascript">
        var recharge_money = {{ recharge_money|raw }};
        window.isMobile = true;
        window.token = "{{ token }}";
    </script>
    {% include 'cdn.html.twig' %}
    <style>
        html {
            font-family: Microsoft Yahei;
            line-height: 1.15;
            -ms-text-size-adjust: 100%;
            -webkit-text-size-adjust: 100%;
            height: 100%;
        }

        body {
            margin: 0;
            height: 100%;
        }

        article,
        aside,
        footer,
        header,
        nav,
        section {
            display: block;
        }

        h1 {
            font-size: 2em;
            margin: 0.67em 0;
        }

        ul,
        ul li {
            list-style: none;
        }

        figcaption,
        figure,
        main {
            display: block;
        }

        figure {
            margin: 1em 40px;
        }

        hr {
            box-sizing: content-box;
            height: 0;
            overflow: visible;
        }

        pre {
            font-family: monospace, monospace;
            font-size: 1em;
        }

        a {
            background-color: transparent;
            -webkit-text-decoration-skip: objects;
        }

        a:active,
        a:hover {
            outline-width: 0;
        }

        abbr[title] {
            border-bottom: none;
            text-decoration: underline;
            text-decoration: underline dotted;
        }

        b,
        strong {
            font-weight: inherit;
        }

        b,
        strong {
            font-weight: bolder;
        }

        code,
        kbd,
        samp {
            font-family: monospace, monospace;
            font-size: 1em;
        }

        dfn {
            font-style: italic;
        }

        mark {
            background-color: #ff0;
            color: #000;
        }

        small {
            font-size: 80%;
        }

        sub,
        sup {
            font-size: 75%;
            line-height: 0;
            position: relative;
            vertical-align: baseline;
        }

        sub {
            bottom: -0.25em;
        }

        sup {
            top: -0.5em;
        }

        audio,
        video {
            display: inline-block;
        }

        audio:not([controls]) {
            display: none;
            height: 0;
        }

        img {
            border-style: none;
        }

        svg:not(:root) {
            overflow: hidden;
        }

        button,
        input,
        optgroup,
        select,
        textarea {
            font-family: Microsoft Yahei;
            font-size: 100%;
            line-height: 1.15;
            margin: 0;
        }

        button,
        input {
            overflow: visible;
        }

        button,
        select {
            text-transform: none;
        }

        button,
        html [type="button"],
        [type="reset"],
        [type="submit"] {
            -webkit-appearance: button;
            /* 2 */
        }

        button::-moz-focus-inner,
        [type="button"]::-moz-focus-inner,
        [type="reset"]::-moz-focus-inner,
        [type="submit"]::-moz-focus-inner {
            border-style: none;
            padding: 0;
        }

        button:-moz-focusring,
        [type="button"]:-moz-focusring,
        [type="reset"]:-moz-focusring,
        [type="submit"]:-moz-focusring {
            outline: 1px dotted ButtonText;
        }

        fieldset {
            border: 1px solid #c0c0c0;
            margin: 0 2px;
            padding: 0.35em 0.625em 0.75em;
        }

        legend {
            box-sizing: border-box;
            color: inherit;
            display: table;
            max-width: 100%;
            padding: 0;
            white-space: normal;
        }

        progress {
            display: inline-block;
            vertical-align: baseline;
        }

        textarea {
            overflow: auto;
        }

        [type="checkbox"],
        [type="radio"] {
            box-sizing: border-box;
            padding: 0;
        }

        [type="number"]::-webkit-inner-spin-button,
        [type="number"]::-webkit-outer-spin-button {
            height: auto;
        }

        [type="search"] {
            -webkit-appearance: textfield;
            /* 1 */
            outline-offset: -2px;
            /* 2 */
        }

        [type="search"]::-webkit-search-cancel-button,
        [type="search"]::-webkit-search-decoration {
            -webkit-appearance: none;
        }

        ::-webkit-file-upload-button {
            -webkit-appearance: button;
            /* 1 */
            font: inherit;
            /* 2 */
        }

        details,
        menu {
            display: block;
        }

        summary {
            display: list-item;
        }

        canvas {
            display: inline-block;
        }

        template {
            display: none;
        }

        [hidden] {
            display: none;
        }

        .container {
            width: 100%;
            height: 100%;
            color: #454645;
        }

        .container .row {
            padding: 15px;
        }

        .container .row .btn {
            margin: 0px 15% 20px 15%;
            width: 70%;
            background-color: #fa0a82;
            font-size: 16px;
            color: white;
            padding: 10px;
            border: 0;
            border-radius: 5px;
            cursor: pointer;
            transition: 0.2s all ease-in;
            -webkit-user-select: none;
            -moz-user-select: none;
            -ms-user-select: none;
            user-select: none;
        }

        .container .row .btn:hover {
            background-color: #d6006b;
        }

        .container .row .short-title {
            text-indent: 5px;
            color: #a9a9a9;
            border-left: 2px solid #fa0a82;
            font-size: 0.8rem;
        }

        .container .title {
            font-size: 0.8rem;
            background-color: #fff;;
        }

        .row-list {
            text-align: center;
        }

        .row-list .row-item {
            float: left;
            width: 21%;
            text-align: center;
            padding: 5px 0;
            position: relative;
            background-color: white;
            box-shadow: 0px 2px 6px 2px #e8e8e8;
            border: 1px solid transparent;
            border-radius: 3px;
            margin-left: 2.5%;
            margin-top: 5px;
            margin-bottom: 5px;
        }

        .row-list .row-item span {
            font-size: 13px;
            display: inline-block;
            width: 100%;
            line-height: 24px;
        }

        .row-list .row-item .row-item_title {
            color: #454645;
            -webkit-user-select: none;
            -moz-user-select: none;
            -ms-user-select: none;
            user-select: none;
        }

        .row-list .row-item .row-item_score {
            color: #fa0a82;
            -webkit-user-select: none;
            -moz-user-select: none;
            -ms-user-select: none;
            user-select: none;
        }

        .row-list .row-item .row-item_icon {
            width: 20px;
            height: 20px;
            display: none;
            position: absolute;
            right: 0;
            bottom: 0;
            z-index: 10;
            background-size: contain;
            background-image: url({{ STATIC_PATH }}/public/css/i/select_stroke.png);
        }

        .row-list .row-item-active {
            border: 1px solid #fa0a82;
            border-radius: 3px;
            background-color: #fff;
        }

        .row-list .row-item-active .row-item_icon {
            display: block;
        }

        .up_options .row-item {
            width: 45%;
        }

        .bg-gray {
            background-color: #fafafa;
        }

        .fl {
            float: left;
        }

        .fr {
            float: right;
        }

        .clear {
            clear: both;
        }

        /* Clear Floats */
        .clearfix:before, .clearfix:after {
            content: '.';
            display: block;
            overflow: hidden;
            visibility: hidden;
            font-size: 0;
            line-height: 0;
            width: 0;
            height: 0;
        }

        .clearfix:after {
            clear: both;
        }

        .clearfix {
            zoom: 1;
        }
        .free{
            color: #a9a9a9;
            clear: both;
            text-align: left;
            font-size: .8rem;
        }
        .red{
            color: #fa0a82;
        }
        .charge-tips {
            padding: 10px 10px 0px 10px;
            font-size: 14px;
            color: #ce0068;
        }

        .price_option_2 {
            text-indent: -100000px;
            background-size: contain;
            background-repeat: no-repeat;
            background-position: center center;
            background-image: url({{ STATIC_PATH }}/public/src/img/qqpay.jpg);
        }

        .price_option_4 {
            text-indent: -100000px;
            background-size: contain;
            background-repeat: no-repeat;
            background-position: center center;
            background-image: url({{ STATIC_PATH }}/public/src/img/yinlian.jpg);
        }

        .price_option_5 {
            text-indent: -100000px;
            background-size: contain;
            background-repeat: no-repeat;
            background-position: center center;
            background-image: url({{ STATIC_PATH }}/public/src/img/alipay.png);
        }

        .price_option_6 {
            text-indent: -100000px;
            background-size: contain;
            background-repeat: no-repeat;
            background-position: center center;
            background-image: url({{ STATIC_PATH }}/public/src/img/wechat.png);
        }

        .loading {
            position: absolute;
            height: 100%;
            width: 100%;
            left: 0px;
            top: 0px;
            background-color: rgba(0, 0, 0, 0.3);
            display: none;
        }

        .loadingImg {
            width: 200px;
            height: 200px;
            top: 50%;
            left: 50%;
            margin-top: -100px;
            position: absolute;
            margin-left: -100px;
            background-size: cover;
            background-image: url({{ STATIC_PATH }}/public/images/loading.gif);
        }
    </style>
    <script type="text/javascript" src="{{ STATIC_PATH }}/public/dest/{{ publish_version }}/cross.js"></script>
    {% include 'google-analytics.html.twig' %}
</head>
<body>

<div class="container bg-gray">
    <div class="row title">
        <span class="fl">充值账号: {{ user.nickname }}</span>
        <span class="fr">余额：{{ user.points }}钻</span>
        <div class="clear"></div>
    </div>
    <div class="row">
        <div class="short-title">充值数量 1元=10钻</div>
    </div>
    <div class="row-list up_selects">
        {% for item in active %}
            <label for="charge{{ item.moneymin }}" class="row-item" data-charge-id="{{ item.moneymin }}" data-fee="{{ item.fee }}" >
                <span class="row-item_title">{{ item.giftname }}</span>
                <span class="row-item_score">{{ item.moneymin }}{{ L('CHARGE.ORDER.ORDER.PRICE_UNIT') }}</span>
                <span class="row-item_icon"></span>
                <div class="charge-diamond"></div>
                <input type="radio" name="price" id="charge{{ item.moneymin }}" data-free="{{ item.fee }}" value="{{ item.moneymin }}"
                       style="display: none">
            </label>
        {% endfor %}
        <div class="row">
            <p class="free"></p>
        </div>
        <div class="clear"></div>
    </div>
    <div class="row">
        <div class="short-title">{{ L('CHARGE.ORDER.ORDER.CHARGE_TITLE_CHANEL') }}</div>
    </div>
    <div class="row-list up_options clearfix pay-type">
        {% for id,type in recharge_type %}
            <label class="row-item" data-id="{{ id }}" for="price_option_{{ id }}">
                <input type="radio" name="vipLevel" id="price_option_{{ id }}" class="chargePlat"
                       value="{{ type.channel }}" style="display: none; visibility: visible">
                <span class="row-item_title price_option_{{ id }}">{{ type.name }}</span>
                <span class="row-item_icon"></span>
            </label>
        {% endfor %}

    </div>
    <div class="charge-tips"></div>
    <div class="row">
        <button class="fl btn" id="chargePay">立即支付</button>
        <div class="clear"></div>
    </div>
</div>
<script  type="text/javascript" >
  //头像图片服务器
  window.IMG_PATH = "{{ IMG_PATH }}";

  //load lib
  cross.importFile("libJs", "js");
  cross.importFile("commonJs", "js");
</script>
<script>
    $(function () {

        $(".up_selects").on("click", ".row-item", function () {
            var _free = $(this).data('fee');
            $(".up_selects >.row-item").removeClass('row-item-active');
            $(this).addClass('row-item-active');
            $('.free').html( _free != 0 ? '*当前金额赠送 <span class="red">' + _free + '</span> 钻' : '' );
        });

        $(".up_options").on("click", ".row-item", function () {
            $(".up_options >.row-item").removeClass('row-item-active');
            $(this).addClass('row-item-active');
        });

        $(".up_selects").find('.row-item').filter("[data-charge-id=50]").trigger("click");
        $(".up_options").find(".row-item").filter("[data-id=5]").trigger("click");

        var markCharge = function (orderId, sucCb) {
            $.ajax({
                url: "/charge/checkCharge",
                data: {"orderId": orderId},
                dataType: "json",
                type: "GET",
                success: function (res) {
                    if (sucCb) {
                        sucCb(res)
                    }
                    ;
                },
                error: function () {

                }
            });
        }

        $(document).on("click", ".charge-unknown", function () {
            var tradeNo = $(this).data("tradeno");
            var dialogFinishState = $.dialog({title: "提示", content: "正在获取状态，请稍等"}).show();

            markCharge(tradeNo, function (res) {
                $.tips(res.msg);
                dialogFinishState.remove();
            });
        });


        //充值页面点击按钮提交充值额度
        $("#chargePay").on("click", function () {

            var $tipsBox = $(".charge-tips");
            var inputPrice = $("#chargeIpt").val();

            var $chargePrice = $("input[name='price']");
            var $chargeChannels = $(".chargePlat");
            var $chargeChannel = $(".chargePlat:checked");
            var chargeId = $chargeChannel.parent("label").data("id");
            var $chargeType = $chargeChannel.parent('label');

            //充值金额选择
            if (!$chargePrice.is(":checked")) {
                $tipsBox.text("请选择要充值的金额");
                return;
            }
            ;

            //平台金额选择
            var isChargeChannelChecked = false;
            $.each($chargeChannels, function (i, e) {

                //if($(e).is(":checked") && $(e).is(":visible")) {
                if ($(e).is(":checked")) {
                    isChargeChannelChecked = true;
                    return false;
                }

            });

            if ($chargeChannels.length == 0 || !isChargeChannelChecked) {
                $tipsBox.text("请选择充值渠道，若无充值渠道，请联系客服。");
                return;
            }

            //radio 表单值
            var radioPrice = $chargePrice.filter(":checked").val(); //Number($chargePrice.filter(":checked").data('free'));

            //充值金额
            var price = Number(radioPrice) != -1 ? radioPrice : inputPrice;

            //获取状态dialog
            var dialogFinishState = $.dialog({title: "提示", content: "正在获取状态，请稍等"});

            //向系统确认dialog
            var makeSureFromSystem;


            //加载全屏loading
            $(".loading").show();

            //请求跳转链接key
            $.ajax({
                url: "/m/pay/pay",
                data: {
                    "price": price,
                    "vipLevel": $chargeChannel.val(),
                    "mode_type": $chargeType.data('id')
                },
                headers: {
                    Authorization: 'Bearer ' + window.token
                },
                dataType: "html",
                type: "POST",
                success: function (res) {
                    //是否已经提交成功
                    //if (!res.status) {
//                        var dialogPayState = $.dialog({
//                            title: "支付状态",
//                            id: "dialogChargeState",
//                            content: "<p>充值金额：" + price + "元<br/></p>",
//                            onshow: function(){
//                                $("#dialogChargeState").on("click", function(){
//                                    markCharge(res.msg.orderId);
//                                });
//                            },
//                            okValue: "已完成支付",
//                            ok: function(){
//
//                                //获取充值状态
//                                var makeSureMarkCharge = function(){
//
//                                    if (typeof makeSureFromSystem != "undefined") {
//                                        makeSureFromSystem.remove();
//                                    }
//
//                                    dialogFinishState.show();
//
//                                    markCharge(res.msg.orderId, function(json){
//                                        //如果返回0，则能够获取状态，即成功，失败，或交易未知
//                                        if(!json.status){
//                                            location.href = "/member/charge";
//                                        }else{
//                                            //如果返回1，即程序内部问题
//                                            makeSureFromSystem = $.dialog({
//                                                title: "提示",
//                                                content: json.msg,
//                                                ok: function(){
//                                                    makeSureMarkCharge();
//                                                    return false;
//                                                },
//                                                okValue: "再次向系统确认",
//                                                cancel: function(){},
//                                                cancelValue: "关闭"
//                                            }).show();
//                                        }
//
//                                        dialogFinishState.remove();
//                                    });
//                                }
//
//                                makeSureMarkCharge();
//                            }
//                        });
//
//                        dialogPayState.show();
                        //if(chargeId == 6){

                            //微信根据后台配置有两种情况, 一种是给出二维码图片, 一种是直接给出支付页面

                            //if(/^http:\/\//g.test(res.msg)){
                                //微信 ( 传入二进制的图片链接 )
                                //设置qrcode url到localstorage，提供给wechat页面调用

                                //因为有重提安全策略的原因, 不适用以下方法, 而是直接打开二维码
                                //localStorage.setItem("qrcode-url", res.msg);
                                //location.href = "/m/pay/wechat?jwt=" + window.token;

                                //location.href = res.msg;
                            //}else{
                                document.write(res);
                            //}

                        //}else if(chargeId == 5){
                            //支付宝 直接打开页面
                            //document.write(res.msg);
                        //} else {
                            //第三方支付
                            //location.href = res.msg.gotourl;
                        //}

                    //}else{
                        //$.tips(res.msg);
                    //};

                    $(".loading").hide();

                },
                error: function (res) {
                    console.log(res);
                    $(".loading").hide();
                }
            });
        });

    });
</script>
<div class="loading">
    <div class="loadingImg"></div>
</div>
</body>
</html>
